---
phase: 02-api-client-rate-limiter
plan: "02-01"
type: execute
---

<objective>
Implement fully-functional RoostooClient replacing the Phase 7 stub: HMAC SHA256 request signing and all 6 exchange endpoints correctly wired.

Purpose: The stub raises NotImplementedError on every call. Main loop and execution engine need a working client before live trading can start.
Output: `bot/api/client.py` with functional HMAC-signed HTTP calls to all Roostoo endpoints.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-scaffolding/1-01-SUMMARY.md
@.planning/phases/01-project-scaffolding/1-02-SUMMARY.md
@bot/api/client.py
@bot/api/__init__.py

**Tech stack available:** requests, python-dotenv, pyyaml (requirements.txt — Phase 1 confirmed)
**Established patterns:** `bot.*` namespace for all imports; empty `__init__.py` files as package markers

**Constraining decisions:**
- POST bodies must be form-encoded (`data=`), NOT JSON (`json=`) — PROJECT.md API auth spec
- HMAC: sort params alphabetically → `key=val&key2=val2` → HMAC-SHA256 hex digest
- Headers: `RST-API-KEY` (API key value), `MSG-SIGNATURE` (signature hex)
- `pending_count` `Success=false` is NOT an error — check `TotalPending` field only; do NOT log WARNING
- `pending_only` parameter must be string `"TRUE"` / `"FALSE"` (not Python bool) — PROJECT.md quirk
- Quantity is in coin units (BTC), not USD
- `get_open_orders()` uses GET `/v3/order` with `pending_only=TRUE`
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HMAC signing helper and internal _request() method</name>
  <files>bot/api/client.py</files>
  <action>
Add imports at top of file (after existing imports):
  import hashlib
  import hmac
  import time
  import urllib.parse
  import requests

Add two methods to RoostooClient (before the public endpoint methods):

  def _sign(self, params: dict) -> str:
      """HMAC-SHA256 sign: sort params alphabetically, urlencode, sign with secret."""
      msg = urllib.parse.urlencode(sorted(params.items()))
      return hmac.new(self.secret.encode(), msg.encode(), hashlib.sha256).hexdigest()

  def _request(self, method: str, path: str, params: dict | None = None) -> dict:
      """Make a signed HTTP request. GET uses query string; POST uses form-encoded body."""
      if params is None:
          params = {}
      signature = self._sign(params)
      headers = {
          "RST-API-KEY": self.api_key,
          "MSG-SIGNATURE": signature,
      }
      url = self.base_url + path
      if method == "GET":
          resp = requests.get(url, params=params, headers=headers, timeout=10)
      else:
          resp = requests.post(url, data=params, headers=headers, timeout=10)
      resp.raise_for_status()
      return resp.json()

Note: backoff/rate-limiting is added in 02-02. This plan wires the signing and dispatch only.
  </action>
  <verify>python -c "from bot.api.client import RoostooClient; c = RoostooClient('k', 's'); sig = c._sign({'pair': 'BTC/USD', 'amount': '1'}); assert len(sig) == 64, f'bad sig len {len(sig)}'; print('sign ok:', sig[:16], '...')"</verify>
  <done>_sign() returns 64-char hex string for known input; _request() method exists; no ImportError or AttributeError</done>
</task>

<task type="auto">
  <name>Task 2: Implement all 6 public endpoint methods</name>
  <files>bot/api/client.py</files>
  <action>
Replace every `raise NotImplementedError(...)` stub with a real implementation:

  def get_ticker(self, pair: str) -> dict:
      """Return ticker dict with 'LastPrice' for the given pair."""
      return self._request("GET", "/v3/ticker", {"pair": pair})

  def get_balance(self) -> dict:
      """Return portfolio balances. Keys: 'total_usd', 'BTC', 'USD'."""
      return self._request("GET", "/v3/balance", {})

  def place_order(self, pair: str, side: str, quantity: float) -> dict:
      """Submit a market order. side must be 'BUY' or 'SELL'. quantity in coin units."""
      return self._request("POST", "/v3/place_order", {
          "pair": pair,
          "side": side,
          "quantity": str(quantity),
          "type": "MARKET",
      })

  def pending_count(self) -> dict:
      """Return raw response containing 'TotalPending'.
      IMPORTANT: Success=false is the NORMAL response for zero pending orders.
      Do NOT log WARNING on Success=false — check TotalPending instead."""
      return self._request("GET", "/v3/pending_count", {})

  def cancel_order(self, order_id: int) -> dict:
      """Cancel an order by ID."""
      return self._request("POST", "/v3/cancel_order", {"order_id": str(order_id)})

  def get_open_orders(self) -> list:
      """Return list of open order dicts.
      pending_only MUST be string 'TRUE' not Python bool True."""
      resp = self._request("GET", "/v3/order", {"pending_only": "TRUE"})
      return resp.get("Data", [])
  </action>
  <verify>python -c "
from bot.api.client import RoostooClient
import inspect
c = RoostooClient('k', 's')
methods = ['get_ticker', 'get_balance', 'place_order', 'pending_count', 'cancel_order', 'get_open_orders']
for m in methods:
    src = inspect.getsource(getattr(RoostooClient, m))
    assert 'NotImplementedError' not in src, f'{m} still has NotImplementedError'
    assert '_request' in src, f'{m} does not call _request'
# Verify pending_count has no WARNING log on Success=false
src = inspect.getsource(RoostooClient.pending_count)
assert 'warning' not in src.lower() or 'Success' not in src, 'pending_count incorrectly logs warning'
print('all 6 methods ok')
"</verify>
  <done>All 6 methods call _request(); none raise NotImplementedError; pending_count has no WARNING log on Success=false; get_open_orders passes pending_only as string "TRUE"</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -c "from bot.api.client import RoostooClient"` succeeds with no errors
- [ ] `python -c "from bot.api import RoostooClient"` succeeds (re-exported via __init__.py)
- [ ] None of the 6 methods raise NotImplementedError
- [ ] POST endpoints use `data=` (form-encoded) not `json=` — inspect _request() source
- [ ] `pending_count()` source has no `logger.warning` triggered by Success=false
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- _request() uses `data=` for POST (form-encoded body), `params=` for GET (query string)
- HMAC signature is deterministic: same params → same hex output
- No ImportError or AttributeError in bot.api namespace
  </success_criteria>

<output>
After completion, create `.planning/phases/02-api-client-rate-limiter/02-01-SUMMARY.md`:

---
phase: 02-api-client-rate-limiter
plan: "02-01"
subsystem: api-client
tags: [python, hmac, requests, exchange-api]
requires:
  - phase: 01-01
    provides: bot.api package importable
provides:
  - RoostooClient with HMAC signing and all 6 endpoints functional
affects: ["02-02-rate-limiter", "05-execution-engine", "07-main-loop"]
tech-stack:
  added: [requests, hashlib, hmac, urllib.parse]
  patterns: ["HMAC-SHA256 alphabetical-sort signing", "form-encoded POST body"]
key-files:
  modified:
    - bot/api/client.py
key-decisions: []
issues-created: []
duration: Xmin
completed: 2026-03-16
---

# Phase 2 Plan 01: RoostooClient Implementation Summary

**[Substantive one-liner — what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `bot/api/client.py` — HMAC signing + all 6 endpoint methods implemented

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 02-02-PLAN.md (rate limiter + backoff integration)
</output>
