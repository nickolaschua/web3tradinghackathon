---
phase: 06-strategy-interface
plan: 01
type: execute
---

<objective>
Create the strategy base types: SignalDirection enum, TradingSignal dataclass with pair as a required positional field (no default), and BaseStrategy ABC.

Purpose: Lock in the interface contract that all execution layers depend on. The critical correctness fix here is making TradingSignal.pair required — the current pair="" default causes all order submissions to silently fail (Issue 10, CRITICAL).
Output: bot/strategy/base.py with correct types, bot/strategy/__init__.py exporting public API.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-strategy-interface/06-CONTEXT.md

**Tech stack available:** Python 3.11, dataclasses (stdlib), abc (stdlib), enum (stdlib)
**Established patterns:** bot.* namespace for all imports; flat module structure under bot/strategy/
**Constraining decisions:**
- TradingSignal.pair MUST be the first field with NO default value — TradingSignal() without pair must raise TypeError at construction
- All bot/ imports use bot.* namespace (e.g. from bot.strategy.base import TradingSignal)
- Strategy stubs return HOLD by default — safe live-trading default before alpha logic is filled in
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bot/strategy/base.py with SignalDirection, TradingSignal, BaseStrategy</name>
  <files>bot/strategy/base.py</files>
  <action>
Create bot/strategy/base.py with three things:

1. SignalDirection enum (stdlib enum.Enum):
   - BUY = "BUY"
   - SELL = "SELL"
   - HOLD = "HOLD"

2. TradingSignal dataclass (stdlib dataclasses.dataclass):
   - pair: str  ← REQUIRED POSITIONAL FIELD, NO DEFAULT. This is the critical fix for Issue 10.
     Do NOT add pair: str = "" or any default. TradingSignal() must raise TypeError.
   - direction: SignalDirection = SignalDirection.HOLD
   - size: float = 0.0  (position size as fraction of portfolio, 0.0–1.0)
   - confidence: float = 0.0  (signal confidence 0.0–1.0, reserved for future use)

   Add a __post_init__ that validates: pair must be a non-empty string, direction must be SignalDirection, size must be 0.0–1.0, confidence must be 0.0–1.0. Raise ValueError with descriptive message if invalid.

3. BaseStrategy ABC (abc.ABC):
   - Abstract method: generate_signal(self, pair: str, features: pd.DataFrame) -> TradingSignal
   - Docstring on the abstract method: explains pair is the tradeable symbol (e.g. "BTC/USD"), features is a DataFrame of computed indicators (all shifted by 1 bar to prevent look-ahead bias), returns TradingSignal with pair matching the input pair argument.

Imports needed: from __future__ import annotations, import enum, import pandas as pd, from abc import ABC, abstractmethod, from dataclasses import dataclass, field.

DO NOT use @dataclass(frozen=True) — stubs may need mutable signals during development.
DO NOT add type: ignore comments.
  </action>
  <verify>python -c "from bot.strategy.base import TradingSignal, SignalDirection, BaseStrategy; s = TradingSignal('BTC/USD'); assert s.pair == 'BTC/USD'; assert s.direction == SignalDirection.HOLD; assert s.size == 0.0; print('OK')"</verify>
  <done>
- TradingSignal("BTC/USD") constructs successfully with HOLD defaults
- TradingSignal() raises TypeError (pair is required)
- TradingSignal("BTC/USD", size=1.5) raises ValueError (size out of range)
- BaseStrategy is importable and is an ABC
  </done>
</task>

<task type="auto">
  <name>Task 2: Export public API from bot/strategy/__init__.py</name>
  <files>bot/strategy/__init__.py</files>
  <action>
Update bot/strategy/__init__.py to export the public interface:

```python
from bot.strategy.base import BaseStrategy, SignalDirection, TradingSignal

__all__ = ["BaseStrategy", "SignalDirection", "TradingSignal"]
```

This enables callers to write:
  from bot.strategy import TradingSignal, SignalDirection, BaseStrategy

rather than reaching into the submodule. Keep existing content (if any __init__.py exists from Phase 1 scaffolding) and just add these imports.
  </action>
  <verify>python -c "from bot.strategy import TradingSignal, SignalDirection, BaseStrategy; print('imports OK')"</verify>
  <done>from bot.strategy import TradingSignal works — no import errors</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] python -c "from bot.strategy.base import TradingSignal; TradingSignal()" raises TypeError (pair required)
- [ ] python -c "from bot.strategy.base import TradingSignal; TradingSignal('BTC/USD')" succeeds
- [ ] python -c "from bot.strategy import TradingSignal, SignalDirection, BaseStrategy" succeeds
- [ ] No syntax errors: python -m py_compile bot/strategy/base.py
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- TradingSignal.pair has NO default — TradingSignal() must raise TypeError
- BaseStrategy is importable as ABC
- No Python errors or import failures
</success_criteria>

<output>
After completion, create .planning/phases/06-strategy-interface/06-01-SUMMARY.md:

---
phase: 06-strategy-interface
plan: "06-01"
subsystem: strategy
provides: [TradingSignal, SignalDirection, BaseStrategy]
affects: ["06-02", "07-main-loop-orchestration"]
key-files:
  - bot/strategy/base.py
  - bot/strategy/__init__.py
key-decisions:
  - "TradingSignal.pair is required positional field with no default (Issue 10 fix)"
  - "SignalDirection enum: BUY/SELL/HOLD"
  - "BaseStrategy ABC: generate_signal(pair, features) -> TradingSignal"
tech-stack:
  added: []
  patterns: ["dataclass with required positional field", "ABC strategy interface"]
---

# Phase 6 Plan 01: Strategy Base Types Summary

**[Substantive one-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `bot/strategy/base.py` - SignalDirection enum, TradingSignal dataclass, BaseStrategy ABC
- `bot/strategy/__init__.py` - Public API exports

## Decisions Made

[Key decisions and rationale]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 06-02-PLAN.md
</output>
